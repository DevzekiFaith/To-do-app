import { FC } from "react";

const Header: FC = () => {
  return (
    <div className="w-full p-3 h-14 bg-gradient-to-r from-orange-500 to-purple-500">
      <h1 className="flex items-center justify-center text-white">This is the header Page</h1>
    </div>
  );
};

export default Header;
